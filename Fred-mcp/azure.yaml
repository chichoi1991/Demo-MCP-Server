# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: fred-mcp-python
metadata:
  template: azd-init@1.15.1
services:
  fred-mcp-python:
    project: .
    host: containerapp
    language: py
    docker:
      path: Dockerfile
