# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: thinqmcp-http-typescript-v2
metadata:
  template: azd-init@1.15.1
services:
  thinqmcp-http-typescript:
    project: .
    host: containerapp
    language: js
    docker:
      path: Dockerfile
      remoteBuild: true
